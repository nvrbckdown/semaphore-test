- name: Configure Nginx
- hosts: server
  become: yes
  vars:
    - user_name: "Hello"

  tasks:
  - debug:
    msg: "System {{ inventory_hostname }} has uuid {{ ansible_product_uuid }} and say {{ user_name }}"
